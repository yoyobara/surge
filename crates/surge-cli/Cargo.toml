[package]
name = "surge-cli"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = { version = "4.6.6", features = ["derive", "env"] }
mlua = { version = "0.12.0", features = ["luajit", "vendored"] }
